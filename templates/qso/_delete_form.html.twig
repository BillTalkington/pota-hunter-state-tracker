<form method="post" action="{{ path('app_qso_delete', {'id': qso.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ qso.id) }}">
    <button class="btn">Delete</button>
</form>
